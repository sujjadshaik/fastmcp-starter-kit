services:
  fastmcp-starter-kit:
    build: .
    container_name: fastmcp-starter-kit
    working_dir: /app
    ports:
      - "8100:8000"
    environment:
      MCP_HOST: "0.0.0.0"
      MCP_PORT: "8000"
